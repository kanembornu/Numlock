# CURRENT TASK

Read `AGENTS.md` and `.ai/PROJECT_CONTEXT.md` first.

## Objective

Describe one concrete NUMLOCK outcome.

## Scope

List every allowed file and explicitly excluded behavior.

## Contracts

- Public response properties and globals to preserve:
- Aggregate/cache invariants:
- Spreadsheet and frontend behavior:

## Validation

- Static and syntax checks:
- Local mock or source-contract checks:
- Apps Script functions to run:
- Deployment/browser checks:
- Diff and Git-status checks:

Report unexecuted evidence as `NOT RUN`. External writes require explicit approval.
